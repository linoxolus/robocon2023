function arm(url) {
    var servos = Array.from(document.querySelectorAll('.item'));
    servos.forEach((servo, index) => {
        servo.addEventListener('change', (e) => {
            // fetch()
        })
    })
}

export default arm;